# نقشه تکمیل رادار ۷ — دوازده نشست

> این فایل در ریشه مخزن می‌نشیند و هر نشست کاری با آن شروع می‌شود.
> ساخته‌شده: ۲۱ سپتامبر ۲۰۲۶ — پس از ممیزی کامل مخزن و اجرای آزمون‌ها.

---

## قواعد ثابت همه نشست‌ها

- هر رفع باگ **اول با یک آزمون قرمز** شروع می‌شود، بعد کد.
- آزمون‌ها بدون شبکه و با داده مصنوعی نوشته می‌شوند.
- هیچ عدد بازاری از جست‌وجوی وب نمی‌آید. فقط اسکریپت مخزن یا اسکرین‌شات کاربر.
- پایان هر نشست: `STATE.md` بخش ۵ و ۶ به‌روز و پوش شود. بدون این، نشست بعد از صفر شروع می‌شود.
- فرمان شروع هر نشست:

```
git pull
pip install -r requirements-dev.txt
python -m pytest -q tests
```

وضعیت امروز: `121` آزمون سبز است. ولی باگ‌های نشست یک هیچ‌کدام آزمون ندارند، یعنی سبزبودن تضمین درستی نیست.

---

# بخش یک — درستی داده. پیش از هر چیز

## نشست ۱ — سه باگ داده و دو برچسب غلط

اندازه: کوچک. یک نشست.

```
فایل STATE.md را بخوان. چهار مورد زیر را یکی‌یکی رفع کن،
هر کدام اول با یک آزمون قرمز در tests/.

۱) خطای یکی‌کم در بلوغ میانگین دویست‌روزه.
   radar_scan.py خط ۶۷ و radar_rotate.py خط ۱۱۷ تعداد ۶۰۰ کندل می‌خواهند.
   کندل باز جدا می‌شود، ۵۹۹ کندل بسته می‌ماند، و شرط بلوغ در خط ۹۴ و ۹۵
   برابر ۶۰۰ است. نتیجه: میانگین دویست‌روزه همه نمادها، حتی بیت‌کوین،
   از ۱۴ سپتامبر «نابالغ» گزارش می‌شود.
   رفع: درخواست ۶۰۱ برای اسکن و چرخش، ۱۵۰۱ در حالت عمیق،
   و یکدست‌کردن مقدار پیش‌فرض ۶۰۰ در radar_levels.py خط ۷۶.
   آزمون: با ۶۰۱ کندل مصنوعی پرچم بلوغ باید صادق باشد؛
   و آزمونی که تعداد درخواستی و آستانه بلوغ را به هم قفل کند.

۲) قدرت نسبی ناهم‌زمان.
   در radar_scan.py حوالی خط ۱۱۲ تا ۱۲۶، بازده کوین از قیمت زنده
   و بازده بیت‌کوین از آخرین کندل بسته حساب می‌شود.
   نشانه عینی: قدرت نسبی بیت‌کوین به خودش در اسکن ۲۰ سپتامبر ‎-۱.۶٪ است.
   همین الگو را در radar_rotate.py خط ۴۴۲ تا ۴۴۴ هم بررسی کن.
   رفع: هر دو طرف از یک مبنا — یا هر دو کندل بسته، یا هر دو زنده.
   آزمون: قدرت نسبی بیت‌کوین به خودش باید دقیقاً صفر باشد.

۳) رژیم ثابت در گردش‌کار روزانه.
   در .github/workflows/radar-daily.yml گام بازبینی سبد با
   --regime "${RADAR_REGIME:--1.0}" اجرا می‌شود و این متغیر هیچ‌جا
   مقدار نمی‌گیرد. پس هر روز رژیم انقباضی به گزارش سبد تزریق می‌شود.
   رفع موقت تا نشست ۲: مقدار از فایل regime.json خوانده شود؛
   اگر فایل نبود یا عمرش بالای هفت روز بود، گزارش صریح بنویسد
   «رژیم کهنه — بازمحاسبه لازم است» و مقدار پیش‌فرض جا نزند.

۴) دو برچسب غلط.
   الف) radar_fetch3.py خط ۱۱۰۰: مثبت‌بودن بازده دوساله منهای سقف نرخ
   «سیاست دیگر انقباضی نیست» خوانده می‌شود. در چرخه افزایش نرخ این یعنی
   بازار افزایش بیشتری را قیمت کرده است. متن خوانش باید به
   «انتظار افزایش بیشتر — باد مخالف دارایی ریسکی» تغییر کند.
   ب) radar_levels.py هیچ سطر «تولید: تاریخ و ساعت» در خروجی ندارد،
   پس قانون تازگی رویش اجراشدنی نیست. یک سطر مهر زمان اضافه کن.

پایان: کل tests را بزن، STATE.md بخش ۵ و ۶ را به‌روز کن، پوش بزن.
```

آزمون پذیرش نشست: در اسکن بعدی، ستون میانگین دویست‌روزه برای بیت‌کوین عدد بدهد نه «نابالغ»، و قدرت نسبی بیت‌کوین به خودش صفر باشد.

---

## نشست ۲ — اسکریپت رژیم

اندازه: متوسط. کار باز `ک۳` که از ۲۲ اوت مانده است.

```
radar_regime.py را بساز.

۱) سه ستون با وزن مشخص:
   نقدینگی جهانی ۴۰ درصد — نرخ و منحنی، نقدینگی خالص فدرال‌رزرو،
     بازده واقعی ده‌ساله، شاخص دلار.
   چرخه بیت‌کوین ۳۵ درصد — میانگین پنجاه‌هفته، تسلط با و بدون استیبل،
     جفت اتر به بیت‌کوین، پهنای بازار.
   اشتهای ریسک ۲۵ درصد — رشد عرضه استیبل، ترس و طمع، تسلط تتر.
۲) قانون سوگیری صفر: جزء غایب صفر نمی‌شود، از مخرج کم می‌شود.
   هر دو امتیاز خام و نرمال‌شده گزارش شوند و باند محافظه‌کارانه‌تر برنده باشد.
۳) خروجی: regime.json شامل امتیاز، نام رژیم، سقف ریسک، ضریب اندازه،
   حداکثر پوزیشن هم‌جهت، مهر زمان، و فهرست اجزای غایب.
   به‌علاوه reports/regime-YYYY-MM-DD.md برای خواندن انسان.

   قرارداد با radar_book.py — از نشست ۱ برقرار است. دو میدان اجباری:
     score         امتیاز **نهایی تصمیم**: باند محافظه‌کارانه‌تر میان
                   امتیاز خام و نرمال‌شده. نه خام، نه نرمال.
     generated_at  زمان ISO با منطقه زمانی. بدون منطقه زمانی نامعتبر است.
   میدان‌های دیگر آزادند؛ خواننده نادیده‌شان می‌گیرد. فایل بالای ۷ روز
   کهنه است و گزارش سبد «رژیم کهنه — بازمحاسبه لازم است» می‌نویسد.
۴) جدول بودجه دقیقاً از SKILL.md گام ۴ بیاید. هیچ سطری صفر نیست.
۵) به گردش‌کار روزانه وصل شود. radar_book.py از نشست ۱ رژیم را از
   regime.json می‌خواند و مقدار پیش‌فرض ندارد — فقط باید فایل ساخته شود،
   پیش از گام بازبینی سبد.
۶) آزمون بدون شبکه با ورودی مصنوعی: سه سناریو — همه اجزا موجود،
   یک ستون کاملاً غایب، و امتیاز مرزی بین دو باند.
```

آزمون پذیرش: گزارش سبد فردا رژیم واقعی و تاریخ‌دار نشان دهد.

---

## نشست ۳ — بستن حلقه پایش

اندازه: متوسط. شامل کار باز `ک۲۰` و `ک۴`.

```
۱) watch.json را بازنویسی کن. الان از ۲۶ اوت مانده و هیچ ابطالی ندارد،
   پس مهم‌ترین ماشه پایشگر خاموش است.
   برای هر نماد سبد و هر نامزد فعال: سطح ابطال ساختاری، نردبان سه‌پله،
   اهداف، و هشدارهای قیمتی. میدان updated هم پر شود.
۲) radar_levels.py را به گردش‌کار روزانه اضافه کن، با خروجی تاریخ‌دار
   در reports/levels-YYYY-MM-DD.md.
۳) holdings.json را با سبد واقعی امروز پر کن — الان یک ماه کهنه است
   و پوزیشنی را دارد که فروخته شده.
۴) نگهبان کهنگی: اگر holdings.json بالای هفت روز یا watch.json بالای
   چهارده روز عمر داشت، گزارش روزانه و پیام تلگرام هر دو صریح بگویند.
۵) کار باز ک۲۱ را پاسخ بده: آیا فاصله چهارساعته برای پایش ابطال کافی است؟
   معیار تصمیم: در سی روز گذشته چند بار قیمت بین دو پالس از سطح ابطال
   رد شده و برگشته. این را از داده واقعی حساب کن، نه از حدس.
```

---

# بخش دو — لایه تحلیل‌گر و چارت ویدیو

> این بخش تقریباً مستقل از بخش یک است. فقط نشست ۷ به خواندن کندل تاریخی وابسته است.

## نشست ۴ — بیدارکردن جمع‌آوری متن

اندازه: کوچک. فقط روی لپ‌تاپ، نه روی رانر گیت‌هاب.

```
۱) requirements-intake.txt بساز با کف و سقف نسخه برای:
   youtube-transcript-api، feedparser، yt-dlp، faster-whisper.
   در README بخش تازه‌ای بنویس: این مسیر فقط محلی اجرا می‌شود،
   چون یوتیوب آی‌پی مرکز داده را می‌بندد.
۲) analysts.yml را بازبینی کن: منبع بدون نشانی خوراک یا تکمیل شود
   یا حذف. نسخه فایل و تاریخ به‌روز شود.
۳) radar_intake.py را یک بار کامل اجرا کن و خروجی را پوش کن.
۴) اگر خطایی داد، ریشه را پیدا کن و آزمون بنویس — ولی هیچ‌چیز را
   با || true خاموش نکن. خرابی بی‌صدا ممنوع است.
```

آزمون پذیرش: `intake/INDEX.md` تاریخ امروز بخورد و دست‌کم یک سند تازه اضافه شود.

---

## نشست ۵ — دفتر ادعا و کارنامه تحلیل‌گر

اندازه: متوسط. **پربازده‌ترین نشست این بخش.**

```
radar_claims.py را بساز، هم‌الگو با radar_journal.py،
با زیرفرمان‌های add، followup، report. ذخیره در analyst_claims.json.

میدان‌های اجباری هر ادعا:
  claim_id، تاریخ انتشار، گوینده، کانال، مکتب، تعارض منافع،
  نماد، جهت، آستانه عددی، مهلت، زمان دقیقه‌وثانیه در ویدیو،
  برچسب معرفتی، و میدان ج۵ — فاصله ادعا از نزدیک‌ترین سقف یا کف پیشین،
  هم بر حسب درصد و هم بر حسب روز.

قاعده ج۵ در method-library.md نوشته شده ولی هیچ‌جا اجرا نمی‌شود.
منشأش این درس است: یک تحلیل دو روز پس از سقف تاریخی گفت دلیلی برای
بستن نیست و بعد ‎-۴۹٪ آمد. هر تحلیل‌گری وسط روند درست می‌گوید؛
آزمون واقعی نقطه عطف است.

followup باید نتیجه ۱۴ و ۳۰ روزه را از کندل واقعی پر کند، نه از متن:
محقق شد یا نه، و اگر معامله می‌شد چند R می‌داد.

report دو کارنامه جدا بدهد: به تفکیک گوینده، و به تفکیک مکتب.
دو منبع از یک مکتب یک رأی‌اند، نه دو رأی.

آزمون: ادعای «۱۲۴ هزار بشکند» از فایل موجود intake/arshia_course
را وارد دفتر کن و بگذار پیگیری‌اش خودکار پر شود.
```

---

## نشست ۶ — `radar_frames.py`

اندازه: متوسط رو به بزرگ. فقط محلی.

```
radar_frames.py را بساز.

۱) ورودی: نشانی ویدیو به‌علاوه فایل متن ساخته‌شده توسط radar_intake.py
   که مهر زمان [MM:SS] و جدول نامزد ادعا دارد.
۲) با yt-dlp فقط بازه‌های لازم را در ۱۰۸۰p بگیر، نه کل ویدیو.
۳) فریم در دو حالت: الف) ثانیه هر نامزد ادعا، با یک فریم قبل و یک بعد؛
   ب) تشخیص تغییر صحنه با ffmpeg برای لحظه عوض‌شدن چارت یا تایم‌فریم.
۴) فریم‌های تقریباً یکسان با اثر انگشت ادراکی حذف شوند.
   سقف خروجی هر ویدیو ۲۵ فریم باشد.
۵) خروجی: پوشه frames/<video_id>/ و یک FRAMES.md که هر تصویر را کنار
   تکه متن همان لحظه می‌گذارد، با شناسه‌ای که به claim_id وصل شود.
۶) هیچ نویسه‌خوانی روی تصویر ننویس. خواندن فریم کار مدل است،
   و عدد غلط از عدد نداشتن بدتر است.
۷) آزمون بدون شبکه: ساختار خروجی، سقف تعداد فریم، و درستی نگاشت
   شناسه فریم به شناسه ادعا.
```

---

## نشست ۷ — خواندن کندل آزاد و پل فریم به داده واقعی

اندازه: متوسط. این همان قابلیت «هر تایم‌فریم و هر بازه تاریخی» است.

```
۱) یک مسیر روشن برای گرفتن کندل در هر تایم‌فریم و هر بازه تاریخی بساز
   — کلید --from و --to و --tf روی radar_fetch3.py یا یک radar_history.py جدا.
   شرط: خروجی همیشه ستون تأیید کندل بسته را داشته باشد.
۲) حالت snap: ورودی نماد، تایم‌فریم، تاریخ ویدیو و یک سطح تقریبی.
   خروجی سه حالت: سطح ساختاری تأیید شد با فلان تعداد برخورد واقعی،
   سطح ساختاری نزدیک وجود ندارد، یا داده کافی نیست.
۳) نتیجه در همان رکورد ادعا ذخیره شود تا بعداً بشود پرسید:
   سطوح این تحلیل‌گر ساختاری بوده‌اند یا خط دلخواه.
۴) آزمون با داده مصنوعی: سطحی با سه برخورد باید تأیید شود،
   سطحی بدون برخورد نباید.
```

---

## نشست ۸ — اتصال لایه تحلیل‌گر به چارچوب ۷

اندازه: کوچک ولی حیاتی.

```
۱) قاعده سخت را در کد و در گزارش بنویس: ادعای تحلیل‌گر هرگز ماشه
   ورود نیست. فقط سه اثر مجاز دارد — کوتاه‌کردن فهرست نامزدها،
   کاهش اندازه، یا اجازه اندازه کامل.
۲) خروجی radar_digest.py و دفتر ادعا باید نمادهای پیشنهادی را به فهرست
   نامزد اسکنر بدهند، و سطوح ادعاشده را به‌عنوان نامزد سطح به radar_levels.py.
۳) هفده قاعده قابل کدنویسی method-library.md را به هفت ستاپ نگاشت کن.
   هر قاعده یا به پیش‌شرط یک ستاپ می‌خورد، یا به لایه ماشه، یا به هیچ‌کدام.
   آن‌هایی که به هیچ‌کدام نمی‌خورند صریح کنار گذاشته شوند، نه نادیده.
```

---

# بخش سه — اسکنر، رویداد، هشدار

## نشست ۹ — اسکنر پامپ واقعی

اندازه: بزرگ. شاید دو نشست.

```
آنچه امروز هست اسکنر چرخش قدرت نسبی است، نه شکارچی پامپ.
سرتیتر گزارشش هنوز «رادار ۵.۴» است و با امتیاز بازنشسته رتبه می‌دهد.

۱) جهان بازار را از ۳۴ جفت بزرگ‌تر کن و اجرا را روزانه کن، نه هفتگی.
۲) سنجه‌های تازه:
   - جهش حجم ساعتی و چهارساعته نسبت به میانگین خود نماد، به شکل عدد
     استانداردشده، نه برچسب «حجم انفجاری».
   - نسبت حجم قرارداد دائمی به حجم نقدی — امضای اصلی پامپ اهرمی.
   - تغییر بهره باز برای همه نمادها، نه فقط هشت نام اول.
   - عمر جفت و فهرست‌شدن تازه.
۳) وتوی خودکار تقویم آزادسازی از نشست ۱۰.
۴) رتبه‌بندی بر پایه حضور شرط ستاپ و فاصله از سطح باشد، نه امتیاز.
۵) نامزدهای برتر خودکار به watch.json و به فهرست نبض اضافه شوند.
   الان نبض فقط سبد و پایش را می‌بیند، پس هر نامزد تازه بدون
   بلوک جریان چهارساعته می‌ماند.
```

---

## نشست ۱۰ — تقویم رویداد، خبر و آزادسازی

اندازه: متوسط.

```
radar_events.py را بساز.

۱) تقویم کلان: جلسات فدرال‌رزرو، تورم، اشتغال. تاریخ‌ها ثابت و
   از منبع رسمی. جلسه بعدی فدرال‌رزرو ۲۷ و ۲۸ اکتبر ۲۰۲۶ است.
۲) تقویم آزادسازی توکن برای هر نماد واچ‌لیست و هر نامزد اسکنر،
   با درصد نسبت به عرضه در گردش.
۳) وتوی خودکار: آزادسازی بزرگ در افق معامله یعنی قاتل ستاپ.
۴) دفتر رویداد بند ۵.۳: هر رویداد با قیمت پیش، و پس از ۲۴ ساعت و ۷ روز.
   بعد از چند ماه، پایگاه اثر رویداد مخصوص همین سبد ساخته می‌شود.
۵) خروجی در گزارش روزانه و در پیام تلگرام بیاید.
```

---

## نشست ۱۱ — تلگرام فاز دو

اندازه: کوچک. راه‌اندازی از قبل کار می‌کند؛ مسئله قابل‌اعتمادبودن است.

```
۱) پیام فقط ۳۵۰۰ نویسه اول LATEST.md است و وسط جدول قطع می‌شود.
   به‌جای بریدن، یک خلاصه مخصوص تلگرام ساخته شود: کارت فشرده،
   تازگی داده، و کوچک‌ترین اقدام درست.
۲) حالت Markdown با متن فارسی و بک‌تیک می‌تواند خطا بدهد و پیام
   اصلاً نرسد. یا به HTML برو یا متن ساده، و نویسه‌های خاص را فرار بده.
۳) پاسخ رابط تلگرام خوانده نمی‌شود، پس ارسال ناموفق بی‌صداست.
   کد وضعیت بررسی و در لاگ گردش‌کار چاپ شود.
۴) وقتی اسکن روزانه خطا می‌دهد، فایل خطا ساخته می‌شود ولی پیام عادی
   می‌رود. خلاصه باید خرابی را صریح اعلام کند.
۵) متن‌های نسخه‌قدیم: LATEST.md هنوز پرسش گشایش ۶.۱ با سه پرسش را
   می‌فرستد و پرسش تازگی داده نسخه ۷ را ندارد.
۶) بخش خودکار STATE.md نسخه فعال را ۶.۱ نشان می‌دهد و کلید تلگرام را
   «تنظیم‌نشده» می‌خواند، چون محرمانه‌ها فقط در سطح مخزن‌اند.
   این بررسی یا حذف شود یا از رابط گیت‌هاب بپرسد.
```

---

## نشست ۱۲ — بدهی‌های کوچک و فایل‌های مرجع

اندازه: متوسط.

```
۱) radar_optcost.py add هنوز میدان شناسه تصمیم و نام ستاپ ندارد و
   میدان امتیاز بازنشسته را دارد. همان کاری که برای دفترچه انجام شد
   اینجا هم انجام شود.
۲) radar_book.py هنوز ضربه را از کاهش امتیاز می‌شمارد. به دو معیار
   نسخه ۷ تغییر کند: بدترشدن ساختار روزانه، و منفی‌ماندن قدرت نسبی
   سی‌روزه به بیت‌کوین. هر هفته هر دو یعنی یک ضربه، فقط یکی یعنی نیم ضربه.
۳) کار باز ک۱۴: کلید --out در radar_fetch3.py پوشه می‌سازد نه فایل.
۴) کار باز ک۱۲: ستون درصد سود و زیان در بازبینی سبد.
۵) نُه فایل مرجع اسکیل نوشته و در references/ گذاشته شوند:
   risk-budget، regime-playbooks، exit-engine، macro-liquidity،
   derivatives، calibration، data-checklist، analyst-lenses، toolchain.
   الان فقط setup-library.md هست. بعد بسته اسکیل دوباره بارگذاری شود.
۶) کار باز ک۵: سنجش اعتبار در افق ۷، ۱۴ و ۹۰ روزه.
```

---

## تصمیم‌هایی که کد حلشان نمی‌کند

این دو مورد منتظر تصمیم انسان‌اند، نه منتظر نشست کاری:

- **تضاد بودجه و سبد موروثی.** سبد اسپات با ابطال‌های دور، حرارت را همیشه چند برابر سقف رژیم نگه می‌دارد، پس وتوی دوم دائمی می‌شود و چارچوب عملاً خاموش می‌ماند — همان چیزی که اصل ۵ منع می‌کند. دو راه: بودجه جدا برای «دفتر موقعیت» و «دفتر معامله»، یا بازتعریف حرارت برای پوزیشن‌های قدیمی.
- **ذخیره استیبل.** کار باز `ک۱۱`، هنوز باز.

---

## ترتیب پیشنهادی هفته‌ها

- هفته یک: نشست ۱، ۲، ۳ — بدون این‌ها هر خروجی دیگری مشکوک است.
- هفته دو: نشست ۴، ۵، ۶ — لایه تحلیل‌گر و فریم چارت.
- هفته سه: نشست ۷، ۸، ۹.
- هفته چهار: نشست ۱۰، ۱۱، ۱۲.
