# دفترچه عملیاتی رادار ۵.۳

> کجا چه کار می‌کنم — کامپیوتر، وب، موبایل

---

## اصل حاکم

**داده از کامپیوتر می‌آید. تحلیل از گفتگو.**

اسکریپت‌ها عدد می‌سازند. چارچوب رادار ۵.۳ که به‌صورت اسکیل نصب است، آن عددها را تفسیر می‌کند.

هیچ‌کدام بدون دیگری کامل نیست.

| محیط | چه می‌تواند |
|---|---|
| کامپیوتر (پاورشل) | همه اسکریپت‌ها، همه داده |
| اپلیکیشن یا وب | تحلیل، چسباندن خروجی، خواندن از گیت‌هاب |
| موبایل | فقط گفتگو و خواندن. اسکریپت اجرا نمی‌شود |

---

## پیش از هر جلسه — همیشه

```
cd $HOME\Documents\radar
git pull
$env:PYTHONUTF8="1"
```

هر بار پاورشل تازه باز می‌کنی، این سه خط اول.

---

# کار ۱ — تحلیل عمیق یک کوین

**در پاورشل:**

```
python radar_fetch3.py ZEC --balance 800 --profile position --venues okx,gate --deep --out zec.txt
```

| بخش | معنی |
|---|---|
| `ZEC` | نماد کوین |
| `--balance 800` | موجودی، برای محاسبه اندازه موقعیت |
| `--profile` | `trade` برای معامله کوتاه، `position` برای نگهداری |
| `--deep` | تحلیل کامل‌تر |
| `--out zec.txt` | ذخیره در فایل |

**بعد:**

```
notepad zec.txt
```

محتوا را کپی کن و در گفتگو بچسبان با این جمله:

> با رادار ۵.۳ تحلیل کن. پروفایل موقعیت.

**راه دوم — بدون کپی:**

```
git add . ; git commit -m "zec data" ; git push
```

بعد در گفتگو فقط بگو «فایل `zec.txt` را بخوان».

---

# کار ۲ — بررسی واچ‌لیست

```
python radar_scan.py --preset all --venues okx,gate --top 5 --out scan.txt
```

**پریست‌های آماده:**

| نام | کوین‌ها |
|---|---|
| `main` | BTC، ETH، SOL |
| `watch` | ۹ کوین اصلی |
| `all` | هر ۱۳ کوین واچ‌لیست |

**یا فهرست دلخواه:**

```
python radar_scan.py --watchlist BTC,ETH,ZEC,HYPE --venues okx,gate --out scan.txt
```

---

# کار ۳ — چرخش و کوین پامپی

```
python radar_rotate.py --venues okx,gate --min-vol 5000000 --top 40 --deep 8 --out rotate.txt
```

| بخش | معنی |
|---|---|
| `--min-vol` | حداقل حجم روزانه — کوین کم‌حجم حذف می‌شود |
| `--top 40` | چند نماد وارد بررسی کندل شود |
| `--deep 8` | برای ۸ نامزد برتر، **آزمون پامپ کاذب** اجرا شود |

`--deep` همان چیزی است که پامپ واقعی را از پامپ ساختگی جدا می‌کند.

**حذف کوینی که نمی‌خواهی:**

```
python radar_rotate.py --venues okx,gate --exclude USDC,USDT --out rotate.txt
```

---

# کار ۴ — نظر تحلیل‌گران

**گرفتن محتوای تازه:**

```
python radar_intake.py --limit 5 --since 2026-08-01
```

**فقط یک منبع:**

```
python radar_intake.py --source joseph_wang --limit 3
```

**ساخت گزارش:**

```
python radar_digest.py --all-roles
notepad intake\DIGEST.md
```

**سه گزارش تولید می‌شود:** واچ‌لیست منابع، قواعد قابل بک‌تست، هم‌پوشانی.

> هشدار: منابع یوتیوبی فقط از کامپیوتر خانگی کار می‌کنند. از کولب مسدودند.

---

# کار ۵ — ژورنال و کالیبراسیون

**ثبت معامله تازه:**

```
python radar_journal.py add --symbol ZEC --side long --entry 515.79 --stop 480 --target 575 --invalidation 480.43 --size 200 --verdict "بدون ورود" --decision "ورود" --rr 1.7 --regime -1.25 --note "خلاف حکم چارچوب"
```

میدان `--verdict` و `--decision` قلب کالیبراسیون‌اند: **حکم چارچوب در برابر تصمیم تو.**

**دیدن معاملات باز:**

```
python radar_journal.py list
```

**بستن معامله:**

```
python radar_journal.py close --id 1 --exit 560 --reason "هدف اول" --lesson "..."
```

**گزارش کالیبراسیون:**

```
python radar_journal.py report
```

---

# روتین پیشنهادی

## هر روز — ده دقیقه

```
cd $HOME\Documents\radar; git pull; $env:PYTHONUTF8="1"
python radar_journal.py check
python radar_scan.py --preset watch --venues okx,gate --out scan.txt
```

بعد در گفتگو: «اسکن امروز را با رادار بررسی کن» و محتوا را بچسبان.

**و قبل از هر تصمیم، این سؤال:** آیا امروز اصلاً نیاز به ورود هست؟ اگر نه، ببند و برو.

## هر هفته — نیم ساعت

```
python radar_rotate.py --venues okx,gate --deep 8 --out rotate.txt
python radar_intake.py --limit 5 --since <تاریخ هفته قبل>
python radar_digest.py --all-roles
python radar_journal.py report
```

## پس از هر معامله

```
python radar_journal.py add ...
git add . ; git commit -m "trade" ; git push
```

---

# جدول محیط‌ها

| کار | کامپیوتر | اپلیکیشن یا وب | موبایل |
|---|---|---|---|
| تحلیل عمیق کوین | ✅ | تفسیر | تفسیر |
| اسکن واچ‌لیست | ✅ | تفسیر | تفسیر |
| چرخش و پامپ | ✅ | تفسیر | تفسیر |
| جمع‌آوری تحلیل‌گران | ✅ | ❌ | ❌ |
| ژورنال | ✅ | ❌ | ❌ |
| گفتگو و آموزش | ✅ | ✅ | ✅ |
| خواندن گزارش | ✅ | از گیت‌هاب | از گیت‌هاب |

**روی موبایل:** داده تازه نمی‌سازی. ولی می‌توانی آخرین گزارش را از گیت‌هاب بخوانی و درباره‌اش گفتگو کنی.

---

# پنج خطای رایج

| خطا | نشانه | راه‌حل |
|---|---|---|
| فراموشی `PYTHONUTF8` | خطای cp1256 | `$env:PYTHONUTF8="1"` |
| فراموشی `git pull` | کد قدیمی اجرا می‌شود | اول هر جلسه pull |
| فراموشی `git push` | سیستم دیگر قدیمی می‌ماند | آخر هر جلسه push |
| دادن داده بدون گفتن پروفایل | تحلیل با وزن اشتباه | همیشه بگو معامله یا موقعیت |
| اجرای یوتیوب روی کولب | مسدود | فقط کامپیوتر خانگی |

---

# قانونی که نباید بشکند

**قانون داده-اول:** هیچ تحلیلی بدون داده شروع نمی‌شود.

اگر بدون خروجی اسکریپت بپرسی «نظرت درباره فلان کوین چیه؟»، جواب درست این است که اول فهرست داده لازم خواسته شود.

استثنا: صریح بگویی «با داده موجود ادامه بده» یا فقط تحلیل ماکرو بخواهی.

---

*ساخته‌شده ۹ اوت ۲۰۲۶ — بر اساس آرگومان‌های واقعی اسکریپت‌ها*
