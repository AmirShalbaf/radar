# رادار ۵.۳ — ابزار واکشی داده

خروجی یک فایل مارک‌داون است که در گفت‌وگو با کلاود می‌چسبانی.

## فایل‌ها

| فایل | کار |
|---|---|
| `radar_scan.py` | اسکن چند کوین، رتبه‌بندی لانگ و شورت |
| `radar_fetch3.py` | تحلیل عمیق یک کوین |
| `radar_colab.ipynb` | اجرا از موبایل، بدون نصب |
| `requirements.txt` | کتابخانه‌های اجرا |
| `requirements-dev.txt` | کتابخانه‌های آزمون |

## اجرا

دو فایل وابستگی از هم جدا هستند: `requirements.txt` فقط چیزی را دارد که
برای **اجرای** رادار لازم است، و گردش‌کارهای گیت‌هاب هم تنها همین را نصب
می‌کنند. `requirements-dev.txt` روی آن سوار می‌شود و ابزار **آزمون** را
اضافه می‌کند. اگر فقط رادار را اجرا می‌کنی، دومی را نصب نکن.

```bash
# اجرا
pip install -r requirements.txt

# توسعه و آزمون — شامل موارد بالا هم می‌شود
pip install -r requirements-dev.txt
python -m pytest tests/ -q

# اسکن
python radar_scan.py --preset all --venues okx,gate

# تحلیل عمیق
python radar_fetch3.py XLM --balance 800 --profile trade --venues okx,gate --macro-event 2026-07-29
```

## ویندوز — کدپیج کنسول

کنسول ویندوز پیش‌فرض کدپیج cp1256 دارد. هر اسکریپت رادار که فارسی چاپ کند،
بدون تنظیم UTF-8 با خطای `UnicodeEncodeError` می‌افتد. حتی یک نشانه ✅ در
خروجی آزمون‌ها کافی است که اجرا را نیمه‌کاره بشکند.

راه دائمی — متغیر محیطی کاربر را یک بار تنظیم کن:

```powershell
[Environment]::SetEnvironmentVariable("PYTHONUTF8", "1", "User")
```

بعد پنجره تازه باز کن. پنجره‌های باز مقدار قدیمی را نگه می‌دارند. بررسی:

```powershell
$env:PYTHONUTF8
```

راه موقت، فقط برای همین جلسه:

```powershell
$env:PYTHONUTF8 = "1"
```

`PYTHONUTF8=1` حالت UTF-8 مفسر را روشن می‌کند و هر دو سو را می‌پوشاند: هم
خروجی کنسول، هم خواندن و نوشتن فایل. جایگزین باریک‌تر `PYTHONIOENCODING=utf-8`
است که تنها جریان‌های ورودی و خروجی را درست می‌کند، نه فایل‌ها.

این همان کار باز شماره ۱۳ در `STATE.md` است.

## صرافی‌ها — واقعیت جغرافیایی

| محیط | در دسترس |
|---|---|
| گوگل کولب (سرور آمریکا) | `okx,gate` — بایننس ۴۵۱، بای‌بیت ۴۰۳ |
| لپ‌تاپ + وی‌پی‌ان غیرآمریکایی | `binance,okx,gate,bybit` |
| لپ‌تاپ بدون وی‌پی‌ان از ایران | `okx,gate` |

`okx,gate` پوشش کامل ۵ از ۵ می‌دهد. گیت تنها منبع رایگان تفکیک نهنگ و خرده‌فروش است.

## کلیدها — همه اختیاری

```bash
export COINGECKO_API_KEY=...   # سقف نرخ از ۱۵ به ۳۰ درخواست در دقیقه
export COINGLASS_API_KEY=...   # لیکوئیدیشن
```

ماکرو از CSV فدرال‌رزرو سنت‌لوئیس می‌آید و **کلید نمی‌خواهد**.

⚠️ کلید را داخل کد ننویس — مخزن عمومی است.

## سنجه‌های نسخه ۳.۲

| بخش | چه می‌دهد |
|---|---|
| اندیکاتور | EMA ۲۰/۵۰/۲۰۰، RSI ۱۴ وایلدر، ATR ۱۴ RMA، MACD — از کندل خام |
| پروفایل حجم | سه لنگر + آزمون هم‌گرایی. حداقل ۲۰ و حداکثر ۱۲۰ کندل |
| فیبوناچی | فقط لایه تأیید. آزمون هم‌نشینی با نقطه کنترل |
| قدرت نسبی | دو پنجره ۳۰ و ۷ روزه، هم‌ترازی تاریخی، باند مرده ±۱٪ |
| امتیاز شورت | شش سنجه مستقل — معکوس امتیاز لانگ نیست |
| ماکرو | فاصله از خنثی، شیب منحنی، بازده واقعی، نقدینگی خالص، ترکیب اشتغال |
| رژیم | تسلط بیت‌کوین با و بدون استیبل، تسلط تتر، TOTAL2/3، ترس و طمع |
| مشتقات | فاندینگ نرمال‌شده ۸ ساعته، بهره باز، لانگ/شورت، تفکیک نهنگ |

## آنچه نمی‌آورد

نقشه گرمایی لیکوئیدیشن، جریان خالص صرافی، اخبار. اینها دستی یا با جست‌وجوی کلاود.
