# وضعیت پروژه رادار — حافظه دائمی

> **این فایل اولین چیزی است که هر جلسه کاری روی رادار خوانده می‌شود.**
> نشانی خواندن مستقیم:
> `https://raw.githubusercontent.com/AmirShalbaf/radar/main/STATE.md`

آخرین به‌روزرسانی دستی: ۲۵ سپتامبر ۲۰۲۶

---

## ۰ — پروتکل اجباری شروع جلسه

هر جلسه‌ای که درباره رادار، تحلیل کوین، سبد یا کد است، **با این ترتیب شروع می‌شود**:

| گام | کار | چرا |
|---|---|---|
| ۱ | همین فایل را از مخزن بخوان | وضعیت واقعی، نه حدس |
| ۲ | `SESSION-2026-08-22.md` را بخوان | کارهای نیمه‌تمام، تصمیم‌های باز و ثبت کامل جلسه ۲۲ اوت |
| ۳ | `reports/LATEST.md` را بخوان | آخرین داده رابط صرافی |
| ۴ | اگر گزارش کهنه بود، **از کاربر بخواه اسکریپت را بزند** | داده تازه |
| ۵ | فقط پس از آن، تحلیل | — |

**ممنوع مطلق:** گرفتن قیمت، اندیکاتور یا هر عدد بازار از جست‌وجوی وب.

جست‌وجوی وب فقط برای این چهار چیز مجاز است: خبر، کاتالیزور، تقویم آزادسازی توکن، رویداد نظارتی. **هرگز برای عدد.**

**دلیل عینی — آزمون‌شده در ۲۲ اوت ۲۰۲۶:** در یک جست‌وجوی واحد، قیمت بیت‌کوین از منابع مختلف چنین برگشت:

| منبع | عدد | وضعیت |
|---|---|---|
| کوین‌مارکت‌کپ | ۷۸٬۵۰۸ | نزدیک واقعیت |
| کوین‌گکو | ۷۷٬۱۱۶ | نزدیک واقعیت |
| اوکی‌اکس | ۶۹٬۱۴۴ | **سه روز کهنه** |
| بایننس | ۹۶٬۲۶۲ | **صفحه کش‌شده، کاملاً غلط** |

پراکندگی بیش از ۲۷ هزار دلار. وب برای عدد بی‌اعتبار است.

---

## ۱ — مسیرهای داده، آزمون‌شده

| مسیر | وضعیت | آزمون |
|---|---|---|
| رابط اوکی‌اکس از محیط کلاود | ❌ **۴۰۳** | ۲۲ اوت ۲۰۲۶ |
| رابط گیت‌آی‌او از محیط کلاود | ❌ **۴۰۳** | ۲۲ اوت ۲۰۲۶ |
| `raw.githubusercontent.com` از محیط کلاود | ✅ **۲۰۰** | ۲۲ اوت ۲۰۲۶ |
| رابط اوکی‌اکس از کامپیوتر کاربر | ✅ کار می‌کند | — |
| رابط بایننس و بای‌بیت از کولب | ❌ ۴۵۱ و ۴۰۳ | — |
| یوتیوب از هر سرور ابری | ❌ مسدود | — |

### نتیجه معماری

```
کامپیوتر کاربر ──اسکریپت──> رابط صرافی ──> reports/ ──git push──> گیت‌هاب ──> کلاود می‌خواند
```

**کلاود هرگز مستقیم به صرافی وصل نمی‌شود. همیشه از راه مخزن.**

---

<!-- شروع بخش خودکار — دستی ویرایش نکن -->

## ۲ — نسخه و فایل‌ها

تولید خودکار: 2026-09-15 20:42 UTC

| مورد | مقدار |
|---|---|
| نسخه فعال | **رادار ۶.۱** |
| آخرین کامیت | `36662ca` — رقم فارسی یکدست، و سفت‌کردن الگوهایی که باید اسکی بمانند |
| شاخه | main |

| اسکریپت | نسخه | موجود |
|---|---|---|
| `radar_fetch3.py` | 3.6 | ✅ |
| `radar_scan.py` | — | ✅ |
| `radar_rotate.py` | — | ✅ |
| `radar_levels.py` | 1.1 | ✅ |
| `radar_journal.py` | — | ✅ |
| `radar_intake.py` | 1.4 | ✅ |
| `radar_digest.py` | 1.1 | ✅ |
| `radar_size.py` | 6.1 | ✅ |
| `radar_book.py` | 6.1 | ✅ |
| `radar_optcost.py` | 6.1 | ✅ |
| `radar_watch.py` | 6.1 | ✅ |
| `radar_validate.py` | 6.1 | ✅ |
| `radar_state.py` | 6.1 | ✅ |

## ۳ — وضعیت راه‌اندازی

| مورد | وضعیت |
|---|---|
| گردش‌کار در `.github/workflows/` | ✅ فایل هست |
| پوشه `reports/` | ✅ تازه — 73 فایل، آخری `book-2026-09-14.md` |
| `holdings.json` (سبد واقعی) | ✅ 20.4 روز پیش |
| `watch.json` (فهرست پایش زنده) | ✅ 0.2 روز پیش |
| `radar_journal.json` (دفترچه معاملات) | ✅ 10.3 روز پیش |
| `radar_optcost.json` (دفتر هزینه فرصت) | ✅ 10.3 روز پیش |
| `book_state.json` (تاریخچه ضربه‌ها) | ✅ 0.2 روز پیش |
| کلید تلگرام | ❌ تنظیم نشده |
| تعداد معامله ثبت‌شده | 1 واقعی + 0 فرضی — ⚠️ حداقل لازم ۲۰ واقعی |
| تعداد رکورد هزینه فرصت | 1 — ⚠️ حداقل لازم ۱۵ |

<!-- پایان بخش خودکار -->

## ۴ — یافته‌های اثبات‌شده

### ۴.۱ — ساختار تصمیم ۵.۴ معیوب بود

| سنجه | مقدار |
|---|---|
| نرخ عبور هفت شرط سخت | حدود ۴.۲٪ |
| نرخ عبور چهار از شش شرط نرم | حدود ۳۴.۴٪ |
| **نرخ عبور کل** | **حدود ۱.۵٪** |
| R/R ضمنی اصل یک‌سوم | ۳.۰، نه ۲.۰ که اعلام شده بود |

### ۴.۲ — سنجش اعتبار موتور امتیازدهی — ۲۲ اوت ۲۰۲۶

نُه نماد، افق ۳۰ روزه، ۵٬۳۶۰ مشاهده خام.

| یافته | مقدار |
|---|---|
| ضریب اطلاعات، بازده مطلق | ‎+۰.۰۳۹ |
| **ضریب اطلاعات، نسبت به بیت‌کوین** | **‎−۰.۰۰۲** |
| **نمونه مؤثر واقعی** | **حدود ۲۴** (پس از تصحیح هم‌پوشانی و همبستگی) |
| خطای معیار با آن نمونه | ‎±۰.۲۰ |

**سه یافته کلیدی:**

۱. **رابطه یکنواخت نیست، U شکل است.** سطل «بسیار منفی» دومین بازده بهتر را داشت (‎+۲.۲۱٪). یعنی بازگشت به میانگین، نه ادامه روند.

۲. **میانه در هر شش سطل منفی است**، ولی میانگین در پنج سطل مثبت. بازده از چند جهش بزرگ می‌آید، نه از عملکرد معمول.

۳. **نمونه برای هیچ حکمی کافی نیست.** صفر داخل بازه اطمینان است.

**حکم:** موتور امتیازدهی کوین‌ها را رتبه‌بندی نمی‌کند. نه برتری اثبات شد، نه نبود آن.

### ۴.۳ — چه بخشی از رادار زنده ماند

| بخش | وضعیت | دلیل |
|---|---|---|
| ورود پلکانی و اصل فاصله ورود تا ابطال | ✅ معتبر | ریاضی محض است |
| بودجه ریسک رژیمی | ✅ معتبر | درباره اندازه، نه انتخاب |
| ابطال ساختاری و خروج اجباری | ✅ معتبر | مدیریت ضرر |
| حرارت واقعی سبد | ✅ معتبر | حسابداری |
| **رده کیفیت A تا F** | ⛔ مشکوک | به امتیاز وابسته |
| **قانون سه‌ضربه** | ⛔ مشکوک | سه کاهش در عدد نویزی، خودش نویز است |
| **آزمون جانشینی** | ⛔ مشکوک | مقایسه دو امتیاز بی‌معنا |

### ۴.۴ — وضعیت واقعی سبد، ۲۲ اوت ۲۰۲۶

| نماد | ورود | فعلی | سود/زیان٪ | ابطال | برای سربه‌سر |
|---|---|---|---|---|---|
| SOL | 158.38 | 93.81 | ‎−۴۰.۸٪ | 72.79 | ‎+۶۸.۸٪ |
| ETH | 3119.59 | 2427.48 | ‎−۲۲.۲٪ | 1715.37 | ‎+۲۸.۵٪ |
| BNB | 748.97 | 694.79 | ‎−۷.۲٪ | 581.68 | ‎+۷.۸٪ |
| TAO | 447.96 | 223.72 | ‎−۵۰.۱٪ | 190.60 | ‎+۱۰۰.۲٪ |
| HYPE | 32.72 | 77.21 | ‎+۱۳۶.۰٪ | 58.07 | — |
| SUI | 1.8600 | 0.8303 | ‎−۵۵.۴٪ | 0.6731 | ‎+۱۲۴.۰٪ |
| XRP | 2.5855 | 1.5010 | ‎−۴۱.۹٪ | 1.0617 | ‎+۷۲.۳٪ |
| AAVE | 262.24 | 125.28 | ‎−۵۲.۲٪ | 88.64 | ‎+۱۰۹.۳٪ |
| ONDO | 0.8255 | 0.3709 | ‎−۵۵.۱٪ | ندارد | ‎+۱۲۲.۶٪ |

---

## ۵ — دفترچه رویدادها

| # | تاریخ | رویداد | درس |
|---|---|---|---|
| ۱ | ۵ اوت ۲۰۲۶ | ورود ZEC در ۵۱۵.۷۹ خلاف حکم چارچوب («بدون ورود»، نسبت ۱.۴۱) | ثبت‌شده |
| ۲ | نامعلوم | **استاپ ZEC در حدود ۴۷۸ خورد** | نیاز به بررسی: آیا استاپ داخل نویز بود؟ |
| ۳ | ۲۰ اوت ۲۰۲۶ | بیت‌کوین از فشردگی شش‌هفته‌ای بالای ۷۱ هزار شکست، سه میلیارد دلار لیکوئید شورت | رژیم عوض شد — امتیاز ماکرو باید بازمحاسبه شود |
| ۴ | ۲۲ اوت ۲۰۲۶ | زی‌کش از ۸۰۰ دلار گذشت، پس از استاپ‌خوردن کاربر | مهم‌ترین رکورد کالیبراسیون تا امروز |
| ۵ | ۲۲ اوت ۲۰۲۶ | رژیم از بحرانی به **انقباضی** رفت. امتیاز ‎−۰.۶۰. بودجه ۲.۵٪، ضریب ۰.۳۰ | عدد ‎−۱.۲۵ منسوخ شد |
| ۶ | ۲۲ اوت ۲۰۲۶ | درس استاپ ZEC: فاصله ۱.۲۸ برابر ATR بود. ایراد اصلی نسبت ۱.۴۱ در ورود بود، نه جای استاپ | با نردبان ورود ضرر حدود ۶۰٪ کمتر می‌شد |
| ۷ | ۲۲ اوت ۲۰۲۶ | برای هشت پوزیشن سطح ابطال نوشته شد. حرارت از ۱۴۵.۸٪ به حدود ۳۵٪ | ONDO سطح معتبر ندارد |
| ۸ | ۲۲ اوت ۲۰۲۶ | RSI شش پوزیشن بالای ۸۰، لنگرهای پروفایل حجم همه پراکنده | هیچ‌کدام ستاپ ورود نیستند |
| ۹ | ۲۲ اوت ۲۰۲۶ | **وضعیت واقعی سبد از اسکرین‌شات:** سرمایه اولیه ۳٬۰۱۲، دارایی ۲٬۰۷۰، ضرر ‎−۹۴۱ یعنی ‎−۳۱.۳٪ | پامپ حدود ۱۸ واحد از ضرر ۴۹ درصدی قبلی را برگرداند |
| ۱۰ | ۲۲ اوت ۲۰۲۶ | **حداکثر ضرر اضافی تا سطوح ابطال: ۴۸۶ دلار.** بدترین حالت ‎−۴۷.۴٪ از سرمایه | تا دیروز این عدد «تا صفر» بود |
| ۱۱ | ۲۲ اوت ۲۰۲۶ | **HYPE با ۱۸۸ دلار، ‎+۱۳۶٪ در سود. یک انتخاب درست ۱۰٪ ضرر نُه انتخاب را جبران کرد** | مسئله انتخاب نیست، اندازه است |
| ۱۲ | ۲۲ اوت ۲۰۲۶ | برای سربه‌سر شدن سبد باید ‎+۴۸.۵٪ رشد کند. SUI و ONDO هر کدام بیش از ‎+۱۲۰٪ لازم دارند | عدم تقارن ضرر و بازگشت |
| ۱۳ | ۳۰ اوت ۲۰۲۶ | **فاز یک رادار ۷ مستقر شد:** `radar_snapshot.py` + گردش‌کار نبض چهارساعته (`radar-pulse.yml`) + بازنویسی گردش‌کار روزانه. اجرای محلی موفق: ۱۱ از ۱۱ نماد | قانون تازگی: داده بالای ۵ ساعت یعنی «داده ندارم» |
| ۱۴ | ۳۰ اوت ۲۰۲۶ | **ریشه باگ ۸ پیدا و رفع شد:** `radar_levels.py` کندل باز را در واکشی دور می‌ریخت و «قیمت» تا ۲۸ ساعت کهنه می‌شد (زی‌کش ۶۵۳.۹۹ به‌جای ۸۰۲.۳۱). نمادهای بدون نسبت هم بی‌صدا از گزارش حذف می‌شدند (ETH و HYPE) | قیمت از کندل زنده، ساختار از کندل بسته؛ «داده ندارم» گزارش‌شدنی است نه حذف‌شدنی |
| ۱۵ | ۳۰ اوت ۲۰۲۶ | **باگ ۹ رفع شد:** بخش ۳ گزارش `radar_optcost.py` شرط با R تهی را «منفی» می‌خواند. سه حالت جدا شد: تهی، مثبت، صفر یا منفی | تهی یعنی «داده ندارم» — قانون مادر داده در متن گزارش هم جاری است |
| ۱۶ | ۳۰ اوت ۲۰۲۶ | نخستین آزمون‌های خودکار مخزن در `tests/` ساخته شد — بدون شبکه، با داده مصنوعی. الگو: اول آزمون قرمز، بعد اصلاح | هر تغییر فقط با آزمون پذیرفته می‌شود |
| ۱۷ | ۳۱ اوت ۲۰۲۶ | **ریشه خرابی چندماهه اسکن روزانه پیدا و رفع شد:** خط ۴۷۶ `radar_scan.py` رشته قالب‌بندی تودرتو با گیومه هم‌جنس داشت — قانونی در پایتون ۳.۱۲ محلی (PEP 701)، خطای نحوی در پایتون ۳.۱۱ رانر گیت‌هاب. دو نمونه دیگر از همان الگو در `radar_rotate.py` (خط ۳۲۸) و `radar_scan.py` (خط‌های ۵۲۹–۵۳۰) هم پیدا و اصلاح شد | **کد باید روی نسخه پایتون محیط اجرا آزمایش شود، نه فقط محیط توسعه** |
| ۱۸ | ۳۱ اوت ۲۰۲۶ | `ast.parse(feature_version=(3,11))` این محدودیت خاص توکنایزر را بازنمی‌گرداند — روی کد معیوب اصلی هم بدون خطا رد شد. اسکنر واقعی باید با `tokenize` پشته گیومه‌های باز رشته‌های f را دنبال کند | ابزار راستی‌آزمایی خودش باید راستی‌آزمایی شود، نه فقط فرض گرفته شود |
| ۱۹ | ۱۱ سپتامبر ۲۰۲۶ | **کار باز ک۱۳ حل شد، و ریشه‌اش دقیق‌تر از ثبت قبلی درآمد.** کدپیج کنسول و کدگذاری محلی (Locale) دو چیز جدا هستند: `chcp` عدد ۶۵۰۰۱ می‌داد، ولی `locale.getpreferredencoding()` همچنان cp1256 بود. پایتون کدگذاری خروجی را از **مقصد** می‌گیرد نه از کدپیج: مقصد کنسول یعنی UTF-8، مقصد لوله یا فایل یعنی کدگذاری محلی. برای همین `tests/test_python311_fstrings.py` در اجرای لوله‌ای روی نشانه ✅ با `UnicodeEncodeError` افتاد، در حالی که در کنسول ساده سالم به نظر می‌رسید. `PYTHONUTF8=1` در متغیرهای محیطی کاربر تنظیم شد — هر دو مسیر و نیز خواندن و نوشتن فایل را می‌پوشاند، برخلاف `PYTHONIOENCODING=utf-8` که تنها جریان‌ها را درست می‌کند | کدپیج ۶۵۰۰۱ بودن کنسول تضمین نمی‌کند خروجی لوله‌ای هم سالم است — و لاگ گردش‌کار همیشه لوله است |
| ۲۰ | ۱۳ سپتامبر ۲۰۲۶ | **کار باز ک۱۵ و ک۱۸ رفع شد — ستون تأیید کندل.** تابع `_df` در `radar_fetch3.py` ستون تأیید را برای هر چهار صرافی یک می‌گذاشت، و مسیر اوکی‌اکس با بریدن `r[:6]` پرچم واقعی اندیس ۸ را دور می‌ریخت. نتیجه: صافی کندل بسته در `radar_scan.py`، `radar_rotate.py` و خود `radar_fetch3.py` بی‌اثر بود. تأیید حالا از پرچم صرافی یا از رابطه «زمان باز شدن به‌علاوه طول کندل در برابر اکنون» می‌آید. **خطر پنهان هنگام رفع:** صادق‌شدن ستون، پنج مصرف‌کننده را که تصادفی قیمت زنده می‌گرفتند به قیمت کهنه می‌انداخت — یعنی بازگشت باگ ۸ به سه فایل تازه. هر پنج محل به الگوی `radar_levels.py` منتقل شد. ۱۸ آزمون تازه در `tests/test_confirm_column.py` | **رفع یک باگ بی‌اثر بودن، باگ دیگری را که با آن خنثی می‌شد آزاد می‌کند.** دو خطا می‌توانند هم را بپوشانند؛ هر صافی بی‌اثر باید پیش از فعال‌شدن، مصرف‌کننده‌هایش بازبینی شوند |
| ۲۱ | ۱۳ سپتامبر ۲۰۲۶ | **نگهبان قیمت پوچ — باگ الگویی، نه محلی.** نگهبان به شکل `live_close is None` نوشته شده بود، ولی `float(nan)` مقدار `nan` می‌دهد نه `None`. اثرش بدتر از رد بی‌صدا بود: کلید `vs_ema200` و `vs_ema50` شرطشان فقط روی خود میانگین است، نه روی قیمت — پس با قیمت پوچ **ساخته می‌شدند** و سنجه «موجود» شمرده می‌شد، در حالی که در مخرج نرمال‌سازی می‌ماند. نقض مستقیم قاعده ۲. هر سه محل با هم درست شد: `radar_scan.py`، `radar_rotate.py`، و `radar_levels.py` که اصلاً نگهبان نداشت و منشأ الگو بود. کمک‌تابع `last_close` کنار `_closed` در `radar_fetch3.py`؛ `radar_levels.py` نسخه محلی گرفت تا استقلالش نشکند، با آزمونی که دو نسخه را قفل می‌کند. سنجه `atr_pct` اتفاقی سالم مانده بود چون شرطش `price > 0` دارد و `nan > 0` همیشه نادرست است — دست‌نخورده ماند ولی تکیه‌گاه نیست. ۲۱ آزمون تازه در `tests/test_null_price_guard.py` | **نگهبان `None` برای مقدار عددی کافی نیست. `NaN` از آن رد می‌شود و بدتر از غیبت است، چون سنجه را موجود جا می‌زند** |
| ۲۲ | ۱۵ سپتامبر ۲۰۲۶ | **دو باگ مستقل در گام «ساخت خلاصه امروز» گردش‌کار روزانه.** یک: متن پرسش گشایش در سه جا نوشته شده بود — دو نقطه در `radar_state.py` و یک heredoc دستی در `radar-daily.yml`. وقتی نسخه از ۶.۰ به ۶.۱ رفت فقط دو تای اول به‌روز شد و گردش‌کار ماه‌ها «رادار ۶.۰» به تلگرام می‌فرستاد. دو: گردش‌کار بخش قانون تازگی را با `head -n 6 | tail -n 3` برش می‌زد، به فرض یک‌سطری بودن؛ ولی سه سطر است — قاعده، نتیجه، اقدام — پس جمله همیشه درست همان‌جا قطع می‌شد که نتیجه و اقدام شروع می‌شود. رفع: `radar_state.py --opening` و `radar_snapshot.py --freshness` تنها منبع اصلی شدند و گردش‌کار فقط صدایشان می‌زند. شماره نسخه از ثابت `VERSION` می‌آید. ۱۶ آزمون تازه در `tests/test_report_single_source.py`، به‌علاوه بررسی دستی با `SNAPSHOT.md` ساختگی | **متنی که در دو جا نوشته می‌شود، دیر یا زود از هم جدا می‌افتد. برش بر اساس شماره سطر ثابت، به ساختار فایل مبدأ وابسته و شکننده است** |
| ۲۳ | ۱۵ سپتامبر ۲۰۲۶ | **رقم لاتین در متن فارسی، و تله `\d`.** سقف عمر نبض «5 ساعت» چاپ می‌شد نه «۵ ساعت». کمک‌تابع `fa` در `radar_state.py` بود و دومین مصرف‌کننده باید یا کپی می‌گرفت یا ایمپورت — کپی همان درس سطر ۲۲ بود، پس `radar_text.py` ساخته شد: فقط کتابخانه استاندارد، تا استقلال `radar_levels.py` و `radar_snapshot.py` و `radar_watch.py` نشکند. ایمپورت از `radar_fetch3.py` رد شد چون pandas و numpy و لایه صرافی را می‌آورد. جست‌وجوی کل مخزن **نسخه سومی** هم پیدا کرد: `radar_intake.py` نگاشت رقم داشت و از مال اولیه کامل‌تر بود — رقم عربی (U+0660) را هم می‌گرفت، که نویسه‌ای جدا از رقم فارسی (U+06F0) است. آن به منبع اصلی منتقل شد، نه برعکس. **تله عبارت باقاعده:** `\d` در پایتون رقم فارسی را هم می‌گیرد و `\w` حرف فارسی را. در `radar_one.py` و `radar_intake.py` شناسه یوتیوب با `[\w-]{11}` گرفته می‌شد، پس یک رشته فارسی یازده‌نویسه‌ای شناسه معتبر جا می‌زد؛ به `[A-Za-z0-9_-]` سفت شد. ولی در الگوهای تشخیص عدد داخل متن تحلیل‌گر، همان رفتار **عمدی** است — «۶۲ هزار» باید تطبیق کند — و عمداً دست‌نخورده ماند، با آزمونی که جلوی «اصلاح» اشتباهی‌اش را می‌گیرد. ۱۹ آزمون تازه در `tests/test_persian_digits.py` | **جست‌وجوی الگو در کل مخزن، نسخه‌ای پیدا می‌کند که از نسخه اصلی کامل‌تر است — پیش از یکی‌سازی باید پرسید کدام درست‌تر است، نه کدام قدیمی‌تر. و یک رفتار زبانی در یک جا تله است و در جای دیگر همان چیزی که می‌خواهی؛ یکجا اصلاح‌کردن بدون تفکیک، باگ تازه می‌سازد** |
| ۲۴ | ۱۶ سپتامبر ۲۰۲۶ | **دو باگ لایه دفترچه، به‌علاوه میدان معامله فرضی.** یک: `radar_journal.py` در `journal.json` می‌نوشت، ولی `radar_state.py` و گردش‌کار هفتگی دنبال `radar_journal.json` می‌گشتند. هیچ خطایی بالا نیامد — فقط جدول وضعیت ماه‌ها گفت «دفترچه ساخته نشده» و شمارنده صفر ماند، در حالی که فایل با یک معامله زی‌کش سر جایش بود. گام «گزارش کالیبراسیون» در `radar-weekly.yml` هم به همین دلیل هرگز اجرا نشد، چون نگهبانش `[ -f radar_journal.json ]` بود. نام روی `radar_journal.json` یکدست شد — همان که پنج مصرف‌کننده از قبل انتظار داشتند و هم‌خانواده `radar_optcost.json` است — و فایل با `git mv` جابه‌جا شد تا تاریخچه و آن یک معامله حفظ شود، نه بازنویسی از صفر. مستند بالای فایل هم که می‌گفت «در .gitignore بگذارش» با واقعیت هماهنگ شد: فایل **عمداً** ردیابی می‌شود چون داده کالیبراسیون است، نه فایل موقت. دو: میدانی برای تفکیک معامله فرضی از واقعی نبود. کلید `--paper` اضافه شد با پیش‌فرض نادرست، پس رکورد قدیمی بدون دست‌خوردن معتبر ماند. گزارش کالیبراسیون و شمارنده `STATE.md` حالا دو دسته را **جدا** می‌شمارند — نه مخلوط و نه فرضی حذف‌شده — و آستانه بیست‌تایی بازتنظیم وزن‌ها فقط با معامله واقعی باز می‌شود. ۳۴ آزمون تازه در `tests/test_journal_layer.py`؛ آزمون شمارنده وضعیت فایل را با نام **نویسنده** می‌نویسد و با چشم **خواننده** می‌خواند، پس جدایی دوباره نام را می‌گیرد | **شمارنده‌ای که فایل اشتباه را می‌شمارد، همیشه صفر می‌ماند و هیچ‌وقت خطا نمی‌دهد — سکوت یک سنجه، خودش باید مشکوک باشد** |
| ۲۵ | ۲۴ سپتامبر ۲۰۲۶ | **امتیاز سبد از این تغییر به بعد با کندل بسته حساب می‌شود — نشست ۱ نقشه رادار ۷.** کامیت `5526c40`. پیش از آن `radar_book.py` فقط ۲۶۰ کندل می‌گرفت و میانگین‌های ۲۰ و ۵۰ و ۲۰۰ و شاخص قدرت نسبی (RSI) را با کندل باز امروز می‌ساخت. میانگین ۲۰۰ با ۲۶۰ کندل هرگز بالغ نبود ولی در امتیاز ساختار شمرده می‌شد. حالا ۶۰۱ کندل گرفته می‌شود، اندیکاتور و شمارش بلوغ از کندل بسته می‌آید و قیمت زنده می‌ماند. قدرت نسبی و وزن‌ها و آستانه‌ها دست نخورد. همراه آن نگهبان قیمت پوچ هم به سبد آمد (`4feb3cd`): پیش از آن قیمت پوچ امتیاز یک نماد سالم را در آزمون از ‎+1.10 به ‎-0.95 می‌برد. **پیامد:** امتیاز و ستون RSI در `reports/book-*.md` پیش از این تغییر با گزارش‌های بعد **مستقیم مقایسه‌پذیر نیست**. خطر ضربه ساختگی — قانون سه‌ضربه هر امتیاز را با قبلی می‌سنجد — **مهار شد** (`dc1144a`): میدان `score_basis` در `book_state.json`؛ دوری که مبنا عوض شده فقط خط پایه ثبت می‌کند و تاریخچه مبنای قدیم زیر `archive` بایگانی می‌شود. امتیاز خالی هم دیگر صفر ثبت نمی‌شود و ضربه‌های قبلی را هم صفر نمی‌کند (`da2f2f5`). بازبینی نخستین گزارش پس از پوش: ک۲۷ | **تغییر مبنای محاسبه یک سنجه، سری زمانی آن را دو تکه می‌کند. هر قاعده‌ای که سنجه را با مقدار قبلی‌اش می‌سنجد، باید مرز تغییر مبنا را بداند** |
| ۲۶ | ۲۵ سپتامبر ۲۰۲۶ | **نشست ۱ رادار ۷ — خطای یکی‌کم بلوغ و قدرت نسبی ناهم‌زمان.** اسکن و چرخش ۶۰۰ کندل می‌خواستند؛ کندل باز جدا می‌شد و ۵۹۹ کندل بسته می‌ماند، زیر آستانه ۶۰۰. از ۱۴ سپتامبر میانگین دویست‌روزه همه نمادها، حتی بیت‌کوین، «نابالغ» بود. همین خطا در `radar_fetch3.py` برعکس بود: `snapshot` کندل باز را هم می‌شمرد و با ۵۹۹ کندل بسته «بالغ» می‌گفت. رفع: ثابت‌های `EMA200_MATURE_BARS` و `DAILY_WANT` (۶۰۱) در `radar_fetch3.py`، نسخه محلی قفل‌شده با آزمون در `radar_levels.py` و `radar_book.py`، و متن هشدار بلوغ گزارش‌ها از همان ثابت (`5526c40`، `98ed70e`، `a0735e9`). قدرت نسبی اسکن: بازده کوین از قیمت زنده بود و بیت‌کوین از کندل بسته — بیت‌کوین به خودش در هر دو پنجره -0.8%. حالا هر دو طرف از کندل بسته (`d11b2a5`). در چرخش هم `rel24` ناهم‌زمان بود، ولی چون فقط مرتب‌سازی می‌کرد و کم‌کردن عدد ثابت ترتیب را عوض نمی‌کند، در هیچ خروجی دیده نمی‌شد. آزمون قفل با «صرافی صادق» ساختگی نوشته شد که دقیقاً به اندازه درخواست کندل می‌دهد و آخرینش باز است | **آستانه و تعداد درخواستی دو عدد جدا نیستند؛ یکی باید از دیگری ساخته شود. و عددی که فقط مرتب‌سازی می‌کند می‌تواند ماه‌ها غلط بماند، چون هیچ خروجی‌ای نشانش نمی‌دهد** |
| ۲۷ | ۲۵ سپتامبر ۲۰۲۶ | **رژیم ثابت و خرابی‌های بی‌صدا.** گردش‌کار روزانه هر روز `-1.0` به گزارش سبد تزریق می‌کرد، هفتگی در اجرای زمان‌بندی‌شده هم، و پیش‌فرض خود `radar_book.py` ۰.۰ بود. عدد تزریقی در همان باند «انقباضی» می‌افتاد که آخرین رژیم واقعی (-0.60، ۲۲ اوت) داشت — پس باند غلط نبود، ولی یک ماه کهنه بود و در گزارش دیده نمی‌شد. حالا رژیم از `regime.json` با دو میدان `score` و `generated_at` خوانده می‌شود؛ غایب، خراب یا بالای ۷ روز یعنی «رژیم کهنه — بازمحاسبه لازم است»، بدون مقدار جانشین. قرارداد در بخش نشست ۲ `PLAN-v7.md` نوشته شد (`03c985e`). `\|\| true` دو گام سبد به اعلام خطا و فایل `book-error-*.md` تبدیل شد. نگهبان قیمت پوچ رویداد ۲۱ هم به سبد آمد — آنجا اثرش بدتر بود: امتیاز یک نماد سالم را از +1.10 به -0.95 می‌برد (`4feb3cd`). در همین جست‌وجو سه خرابی بی‌صدای دیگر پیدا شد: ک۲۴، ک۲۵، ک۲۶ | **مقدار پیش‌فرضی که درست به نظر می‌رسد از پیش‌فرض آشکارا غلط خطرناک‌تر است — هیچ‌کس دنبالش نمی‌گردد. و هر `2>/dev/null` یا `\|\| true` یک خرابی بالقوه بی‌صداست** |
| ۲۸ | ۲۵ سپتامبر ۲۰۲۶ | **خوانش وارونه ماکرو و مهر زمان سطوح.** «فاصله سیاست از خنثی» — بازده ۲ساله منهای سقف نرخ بهره — مثبت‌بودنش را «سیاست دیگر انقباضی نیست» می‌خواند. ولی این عدد مسیری است که بازار برای نرخ انتظار دارد: مثبت یعنی افزایش بیشتر قیمت شده، باد مخالف دارایی ریسکی. در اسکن ۲۴ سپتامبر +0.710 بود و خوانش برعکس. حالا ۲ساله با کل محدوده هدف سنجیده می‌شود — سقف و کف، سری تازه `DFEDTARL` — و برچسب «انتظار بازار از مسیر نرخ» است؛ ورودی غایب «داده ناکافی» می‌گیرد (`b47d240`). این سنجه در هیچ امتیاز یا رأی ماکرویی به کار نرفته بود، فقط نمایشی. `radar_levels.py` هم سطر «تولید» گرفت تا قانون تازگی رویش اجراشدنی شود (`fa7986a`) | **نام یک سنجه مدل ذهنی خواننده را می‌سازد. نام غلط، حتی با عدد درست، خوانش غلط تولید می‌کند** |
| ۲۹ | ۲۵ سپتامبر ۲۰۲۶ | **محیط آزمون: پایتون ۳.۱۱ کنار ۳.۱۳.** نسخه 3.11.9 — آخرین نسخه ۳.۱۱ که نصب‌کننده ویندوز دارد — فقط برای کاربر، بدون افزودن به مسیر، نصب شد. اجرا با `py -3.11`؛ `python` همان ۳.۱۳ ماند. روی ۳.۱۱ نامپای 2.4.6 نصب شد و روی ۳.۱۳ نسخه 2.5.2 — رانر گیت‌هاب هم همان 2.4 را می‌گیرد. کل آزمون‌ها در نشست ۱ با هر دو نسخه زده شد: از ۱۲۱ به ۲۰۹ | **آزمون روی نسخه محیط اجرا، نه فقط محیط توسعه — درس رویداد ۱۷ حالا ابزار دائمی دارد** |
> پانویس سطر ۱۰: عدد ۴۸۶ دلار از قیمت‌های ساعت ۱۳:۴۴ محاسبه شده. اجرای بعدی ۴۸۲ داد. اختلاف از حرکت قیمت است، نه از منطق. عدد دفترچه ثبت لحظه تصمیم است و بازنویسی نمی‌شود.

### پرسش باز شماره ۱ — اولویت بالا

استاپ ZEC در ۴۷۸ بود، یعنی ۷.۳٪ زیر ورود ۵۱۵.۷۹.

**آیا آن فاصله از یک میانگین دامنه واقعی روزانه (ATR-14) بیشتر بود؟**

قانون ۶.۵ رادار می‌گوید فاصله استاپ نباید کمتر از یک ATR روزانه باشد. زی‌کش در ۲۲ اوت در ۲۴ ساعت میان ۵۸۹ تا ۸۵۱ نوسان کرد — دامنه ۴۵٪.

| اگر | آن‌گاه |
|---|---|
| فاصله استاپ زیر یک ATR بود | ایراد از **جای استاپ** بود، نه تحلیل. قانون نقض شده |
| فاصله استاپ بالای یک ATR بود | استاپ درست بود، حرکت غیرقابل پیش‌بینی |

**دستور بررسی:** `python radar_levels.py --symbols ZEC` سپس مقدار ATR را ثبت کن.

---

## ۵.۱ — تصمیم معماری: جدول تازه ۸.۲ (کتابخانه ستاپ، رادار ۷ فاز ۲)

منبع: `references/setup-library.md`، بند ۴. جایگزین رده کاوشی امتیازی بند ۸.۲ رادار ۶.۱.
**فقط ثبت تصمیم — بازنویسی خود `SKILL.md` کار جداگانه‌ای است، اینجا دست نخورده.**

| رده | شرط | ضریب |
|---|---|---|
| الف — استثنایی | ستاپ نام‌دار + همه تأییدهای اختیاری + نسبت ≥ ۴ + ورود روی سطح ساختاری | ۲.۰۰ |
| ب — قوی | ستاپ نام‌دار + حداقل یک تأیید + نسبت ≥ ۳ | ۱.۵۰ |
| ج — قابل‌قبول | ستاپ نام‌دار بدون تأیید اضافه + نسبت ≥ ۲.۵ | ۱.۰۰ |
| رد | هیچ ستاپ نام‌داری فعال نیست، یا نسبت زیر ۲.۵ | ۰ |

**دلیل حذف رده کاوشی:** نسبت پاداش به ریسک ۱.۵ در نرخ برد واقعی این بازار (۴۰٪) زیر نقطه سربه‌سر با کارمزد (۱.۶۲) است. پایین‌ترین پله نردبان کیفیت پیشین، از نظر ریاضی پله بازنده بود — و چون آسان‌ترین شرط را داشت، بیشتر از همه هم فعال می‌شد. کف تازه نسبت پاداش به ریسک برای هر ورودی: **۲.۵**.

## ۶ — کارهای باز، به ترتیب اولویت

> **قرارداد شماره‌گذاری:** ردیف‌های این جدول پیشوند **ک** دارند، ردیف‌های
> دفترچه رویدادها (بخش ۵) عدد خالی. پیش از این هر دو جدول عدد خالی داشتند
> و ردیف ۲۰ و ۲۱ در هر دو بود — ارجاع «ردیف ۲۰» مبهم می‌شد. شماره‌ها
> بازیافت نمی‌شوند، حتی پس از بسته‌شدن ردیف.

| # | کار | چرا اول |
|---|---|---|
| ک۱ | یک بار `radar_watch.py --once` بزن و `reports/` را پوش کن | تا این نشود، کلاود کور است |
| ک۲ | پاسخ پرسش باز شماره ۱ (ATR زی‌کش) | تعیین می‌کند درس چیست |
| ک۳ | امتیاز رژیم را بازمحاسبه کن | رژیم پس از ۲۰ اوت عوض شده، عدد ‎−۱.۲۵ منسوخ است. رفع موقت در نشست ۱: `radar_book.py` رژیم را از `regime.json` می‌خواند و بدون آن «رژیم کهنه» می‌گوید. ساخت خود فایل: نشست ۲ |
| ک۴ | `holdings.json` را با سبد واقعی پر کن | حرارت واقعی سبد |
| ک۵ | سنجش اعتبار در افق ۷، ۱۴ و ۹۰ روزه | آیا شکل U پایدار است |
| ک۶ | ثبت استاپ ZEC در دفترچه و دفتر هزینه فرصت | کالیبراسیون |
| ک۷ | کلید تلگرام و فعال‌سازی گردش‌کار | ✅ حل شد — ۱۵ سپتامبر ۲۰۲۶. مسیر هشدار تلگرام فعال شد و با دریافت گزارش خودکار روزانه تأیید عملی گرفت. محرمانه‌ها در سطح مخزن گیت‌هاب ثبت‌اند و عمداً به شل محلی نمی‌رسند. ردیف نگه داشته شد تا ارجاع پایدار بماند |
| ک۱۰ | **تصمیم درباره ONDO** — تنها پوزیشن بدون سطح ابطال معتبر. ‎−۵۵٪ در ضرر، قدرت نسبی ‎−۲۶٪، برای سربه‌سر ‎+۱۲۳٪ لازم دارد | ✅ بسته شد — ۱۳ سپتامبر ۲۰۲۶. پوزیشن فروخته شد، تصمیم دیگر باز نیست. ردیف نگه داشته شد تا ارجاع پایدار بماند |
| ک۱۱ | **تصمیم درباره ذخیره استیبل** — ۶.۲٪ در برابر هدف رژیم ۴۰٪. کسری حدود ۷۰۰ دلار | بالا |
| ک۱۲ | radar_book.py حالا میدان entry را می‌خواند ولی از آن استفاده نمی‌کند. اگر لازم شد، ستون درصد سود و زیان اضافه شود | پایین |
| ک۱۳ | تنظیم محیط: کنسول ویندوز کدپیج cp1256 دارد. هر اسکریپت رادار که فارسی چاپ کند بدون PYTHONIOENCODING=utf-8 با UnicodeEncodeError می‌افتد. راه دائمی: افزودن به متغیرهای محیطی کاربر | ✅ حل شد — ۱۱ سپتامبر ۲۰۲۶. `PYTHONUTF8=1` در محدوده کاربر تنظیم شد و بخش «ویندوز — کدپیج کنسول» به `README.md` افزوده شد. ریشه دقیق‌تر از ثبت بالا بود — دفترچه رویدادها، سطر ۱۹. ردیف نگه داشته شد تا ارجاع پایدار بماند |
| ک۱۴ | radar_fetch3.py کلید --out را پوشه می‌سازد نه فایل. فایل با نام تاریخ‌دار داخلش قرار می‌گیرد. مسیرها را طولانی و پیش‌بینی‌ناپذیر می‌کند | **نشست ۱۱** — از نشست ۱۲ منتقل شد، چون پشتش خرابی بی‌صدای ک۲۴ است. همین رفتار در `radar_scan.py` و `radar_rotate.py` هم هست |
| ک۱۵ | صافی کندل تأییدشده در radar_scan.py و radar_rotate.py بی‌اثر بود: radar_fetch3.py در مسیر صرافی‌ها ستون تأیید را برای همه یک می‌گذاشت | ✅ حل شد — ۱۳ سپتامبر ۲۰۲۶، با درخواست صریح کاربر. تصمیم: کندل باز **جدا شد**، ولی **حذف نشد**. ردیف نگه داشته شد تا ارجاع پایدار بماند |
| ک۱۶ | سنجه عینی فشردگی در اسکریپت (کتابخانه ستاپ، بند ۷.۱) | ستاپ الف‌۳ («فشردگی نوسان پیش از انبساط») بدون آن قابل غربال خودکار نیست |
| ک۱۷ | افزودن فیلد نام ستاپ به `radar_journal.py` (کتابخانه ستاپ، بند ۷.۲) | ✅ در همین نشست انجام شد — فیلدهای `setup_name` و `decision_id` اجباری شدند، رکورد قدیمی زی‌کش پر شد. ردیف نگه داشته شد تا مرجع در فایل منبع هم دیده شود |
| ک۱۸ | اصلاح ستون تأیید در `radar_fetch3.py` (کتابخانه ستاپ، بند ۷.۳) | ✅ حل شد — ۱۳ سپتامبر ۲۰۲۶. همان کار ردیف ک۱۵. ستاپ‌های کتابخانه حالا واقعاً روی کندل بسته می‌ایستند |
| ک۱۹ | بازنویسی بند ۸.۲ در متن اصلی اسکیل (کتابخانه ستاپ، بند ۷.۴) | جدول تازه بخش ۵.۱ همین فایل باید در خود `SKILL.md` هم جایگزین جدول امتیازی شود — کار جداگانه، عمداً در این نشست انجام نشد |
| ک۲۰ | **سطوح `watch.json` از اوت مانده و دیگر معتبر نیستند** — پایشگر هر چهار ساعت سطوح کهنه را می‌سنجد | بالا |
| ک۲۱ | بررسی اینکه آیا فاصله چهارساعته برای پایش ابطال کافی است یا گردش‌کار جداگانه با فاصله کوتاه‌تر لازم است | متوسط |
| ک۲۲ | سقف ۱۰۰۰ کندل گیت: `Gate.candles` در `radar_fetch3.py` صفحه‌بندی ندارد و `min(1000, want)` می‌گیرد، پس حالت عمیق روی گیت هرگز ۱۵۰۱ کندل نمی‌گیرد | متوسط. **شرط رفع:** خروجی باید تعداد واقعی کندل را اعلام کند، نه اینکه بی‌صدا کمتر بگیرد |
| ک۲۳ | دوازده `\|\| true` باقی‌مانده در گردش‌کارها. روزانه: `radar_optcost.py followup` و `report`، `git add`، `git pull`. نبض: `radar_watch.py --once`، `git add`، `git pull`. هفتگی: `radar_rotate.py`، `radar_journal.py report`، `radar_optcost.py followup` و `report`، `git add`. الگوی جایگزین همان گام سبد نشست ۱: `::error::` و فایل خطا، بدون توقف گام‌های بعد | **نشست ۱۱** |
| ک۲۴ | گام «بازبینی سبد با نامزدهای چرخش» در `radar-weekly.yml` نامزدها را از `reports/rotate-DATE.md` می‌خواند، ولی به خاطر ک۱۴ آن مسیر **پوشه** است. خطای `grep` با `2>/dev/null` خاموش می‌شود و `CANDS` همیشه خالی می‌ماند | **نشست ۱۱**. آزمون جانشینی هفتگی احتمالاً هرگز نامزدی نگرفته است |
| ک۲۵ | `load_state` در `radar_book.py` خطا را با `except Exception: pass` می‌بلعد. اگر `book_state.json` خراب شود، تاریخچه ضربه‌ها بی‌صدا از صفر شروع می‌شود | **نشست ۱۱** |
| ک۲۶ | `--regime nan` را argparse می‌پذیرد و `regime_row` آن را بی‌صدا «بحرانی» می‌کند — یک مقدار جانشین بی‌صدا | **نشست ۱۱** |
| ک۲۷ | **پس از پوش نشست ۱، نخستین گزارش سبد بررسی شود:** «رژیم کهنه — بازمحاسبه لازم است» درست نمایش داده شده؛ دور خط پایه `score_basis` اجرا شده — گزارش «این دور خط پایه تازه است» دارد و `book_state.json` میدان `score_basis` و `archive` گرفته؛ و هیچ ضربه ساختگی ثبت نشده — ستون «ض» همه صفر | **بالا — فوری پس از پوش** |
| ک۲۸ | `radar_book.py` میانگین ۲۰۰ نابالغ را هنوز در امتیاز ساختار می‌شمارد و فقط یادداشت می‌گذارد — نقض قاعده ۱. با ۶۰۱ کندل عملاً فقط برای نمادهای تازه پیش می‌آید | **نشست ۱۲**، همراه بازطراحی منطق ضربه |
| ک۲۹ | میانگین دویست‌روزه **چهارساعته** در `radar_fetch3.py` همیشه نابالغ است: `candles_first_ok` برای این تایم فقط ۵۰۰ کندل می‌گیرد و ۴۹۹ کندل بسته می‌ماند. تعداد باید به ۶۰۱ برسد، هم‌الگو با `DAILY_WANT` نشست ۱. دیده‌شده در آزمون پذیرش نشست ۱ | **نشست ۷** — آن نشست درباره گرفتن کندل در هر تایم‌فریم است |
| ک۳۰ | میانگین دویست‌روزه **هفتگی** در `radar_fetch3.py` همیشه نابالغ است: ۲۰۰ کندل گرفته می‌شود و ۶۰۰ هفته حدود ۱۱.۵ سال است | **رفتار عمدی — رفع نمی‌خواهد.** برچسب «نابالغ» صادقانه است و چارچوب برای چرخه از میانگین پنجاه‌هفته استفاده می‌کند. فقط ثبت شد تا کسی آن را باگ نخواند |
| ک۳۱ | رقم لاتین در برچسب بلوغ `snapshot` در `radar_fetch3.py`: `(499 کندل، حداقل 600 لازم)`. طبق قاعده رقم CLAUDE.md آستانه رقم فارسی می‌خواهد؛ تعداد کندل اندازه‌گیری‌شده لاتین می‌ماند | **نشست ۱۲** |

> کارهای ک۸ و ک۹ در ۳۰ اوت ۲۰۲۶ با آزمون رفع شدند — دفترچه رویدادها، سطر ۱۴ و ۱۵. شماره‌ها برای ثبات ارجاع بازیافت نمی‌شوند.

---

## ۷ — قواعد سخت که تغییر نمی‌کنند

**۱. قاعده بلوغ ۳n.** میانگین نمایی دوره n برای بلوغ حدود ۳n کندل لازم دارد. EMA200 یعنی ۶۰۰ کندل. میانگین نابالغ امتیاز نمی‌گیرد و برچسب «نابالغ» می‌خورد — که با «داده ندارم» فرق دارد.

**۲. قانون سوگیری صفر.** جزء غایب را صفر نگذار. هر دو امتیاز خام و نرمال‌شده را حساب کن و محافظه‌کارانه‌تر را بردار.

**۳. جدایی سطح ساختاری از ماشه‌ای.** نقطه ابطال و حد ضرر فقط از سطح روزانه یا بالاتر با حداقل دو برخورد.

**۴. اصل فاصله ورود تا ابطال.** نسبت با نزدیکی ورود به ابطال ساخته می‌شود، نه با تنگ‌کردن استاپ.

**۵. حالت بی‌ساختار.** سقف بالاتر + کف پایین‌تر یعنی بی‌ساختار. حکمش «رأی نده» است، نه «نزولی».

**۶. حداقل فاصله استاپ.** یک میانگین دامنه واقعی روزانه. **این قانون در معامله ZEC احتمالاً نقض شد — بررسی شود.**

**۷. بازتنظیم آستانه‌ها ممنوع** تا رسیدن به بیست معامله ثبت‌شده.

---

## ۸ — سبک نگارش

- همه پاسخ‌ها فارسی. جمله‌ها کوتاه و یک‌جهته.
- اصطلاح انگلیسی فقط داخل پرانتز بعد از معادل فارسی.
- هرگز واژه انگلیسی وسط جمله فارسی یا با پسوند فارسی.
- داده عددی در جدول یا فهرست، نه پاراگراف پیوسته.
- کامنت‌های داخل کد هم فارسی، با همین قواعد.

---

## ۹ — چگونه این فایل به‌روز می‌شود

| بخش | روش |
|---|---|
| بخش ۳ (وضعیت راه‌اندازی) | خودکار با `python radar_state.py` |
| بخش ۵ (دفترچه رویدادها) | دستی، در پایان هر جلسه مهم |
| بخش ۶ (کارهای باز) | دستی |
| بقیه | دستی |
| رفع باگ رمزگذاری | تابع sh() در radar_state.py حالا encoding صریح utf-8 دارد — ۲۲ اوت ۲۰۲۶ |

**قانون پایان جلسه:** هر جلسه‌ای که تصمیمی گرفته شد یا چیزی کشف شد، پیش از پایان، بخش ۵ و ۶ به‌روز و پوش شود. بدون این، جلسه بعد از صفر شروع می‌شود.
