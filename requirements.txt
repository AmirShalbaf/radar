# وابستگی‌های اجرا. ابزار آزمون در requirements-dev.txt است.
#
# هر خط محدوده صریح دارد — کف و سقف. سقف از جهش نسخه اصلی
# محافظت می‌کند؛ کف چیزی را اعلام می‌کند که واقعا آزموده شده.
# نسخه‌های تأییدشده در بازرسی ۶ سپتامبر ۲۰۲۶:
#   requests 2.34.2 | pandas 3.0.5 | numpy 2.5.2

requests>=2.31,<3.0
pandas>=3.0,<4.0
numpy>=2.0,<3.0
