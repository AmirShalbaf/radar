# رادار — دستورالعمل پروژه

ابزار تحلیل ارز دیجیتال. چارچوب فعال: **رادار ۵.۴**.

## زبان و نگارش

- همه پاسخ‌ها فارسی. جمله‌ها کوتاه و یک‌جهته.
- اصطلاح انگلیسی فقط داخل پرانتز بعد از معادل فارسی: «سطح مقاومت (Resistance)».
- هرگز واژه انگلیسی وسط جمله فارسی یا با پسوند فارسی («EMA200ـه») نیاور.
- داده عددی در جدول یا فهرست، نه در پاراگراف پیوسته. دلیل: تداخل راست‌به‌چپ.
- کامنت‌های داخل کد هم فارسی، با همین قواعد.

## معماری

| فایل | کار |
|---|---|
| `radar_fetch3.py` | موتور واکشی. بقیه از آن `import` می‌کنند. `FRAMEWORK` اینجاست |
| `radar_scan.py` | غربال واچ‌لیست. امتیاز لانگ و شورت |
| `radar_rotate.py` | غربال کل بازار. شکار چرخش |
| `radar_levels.py` | سطوح ساختاری. واکشی مستقل، فقط OKX |
| `radar_journal.py` | دفترچه کالیبراسیون |
| `radar_intake.py` / `radar_digest.py` | خط لوله محتوای تحلیل‌گران |
| `radar_frames.py` | استخراج فریم نمودار از ویدئو |

برچسب نسخه از `R.FRAMEWORK` خوانده می‌شود. در فایل‌های دیگر دستی ننویس.

## قواعد سخت — تغییر نده مگر صریح خواسته شود

**۱. قاعده بلوغ ۳n.** میانگین نمایی دوره n برای بلوغ حدود ۳n کندل لازم دارد.
EMA200 یعنی ۶۰۰ کندل. میانگین نابالغ امتیاز نمی‌گیرد و برچسب «نابالغ» می‌خورد —
که با «داده ندارم» فرق دارد و باید جدا گزارش شود.

**۲. قانون سوگیری صفر.** جزء غایب را صفر نگذار. ولی حذف ساده‌اش از مخرج هم
باگ می‌سازد: اگر آن جزء جریمه‌کننده بود، امتیاز **بالا** می‌رود. هر دو امتیاز
خام و نرمال‌شده را حساب کن و محافظه‌کارانه‌تر را بردار.

**۳. وزن‌ها و شرط‌ها قفل‌اند.** هفت شرط سخت، شش شرط نرم، وزن دو پروفایل.
تا بیست معامله ثبت‌شده در دفترچه نرسیده، بازتنظیم ممنوع. قاعده حداقل نمونه.

**۴. جدایی سطح ساختاری از ماشه‌ای.** نقطه ابطال و حد ضرر فقط از سطح روزانه
یا بالاتر با حداقل دو برخورد. زیر تایم روزانه فقط ماشه است، نه سطح.

**۵. اصل فاصله ورود تا ابطال.** نسبت با نزدیکی ورود به ابطال ساخته می‌شود،
نه با تنگ‌کردن استاپ. فاصله ورود تا ابطال باید زیر یک‌سوم فاصله تا هدف اول باشد.

**۶. پرسش دروازه‌ای.** پیش از هر تحلیل ورود: «آیا امروز اصلاً نیاز به ورود هست؟»
پیش‌فرض خیر است.

**۷. حالت بی‌ساختار.** سقف بالاتر + کف پایین‌تر یعنی بی‌ساختار. حکمش
«رأی نده» است، نه «نزولی». ابهام را به نزول ترجمه نکن.

## صرافی‌ها

| محیط | در دسترس |
|---|---|
| کولب و هر آی‌پی مرکز داده | فقط `okx,gate` — بایننس ۴۵۱، بای‌بیت ۴۰۳ |
| آی‌پی خانگی غیرآمریکایی | بایننس هم باز است |
| وی‌پی‌ان آمریکا | بایننس بسته — خاموشش کن |

پیش‌فرض امن: `--venues okx,gate`. فقط OKX و Gate تفکیک نهنگ و خرده‌فروش رایگان دارند.

یوتیوب آی‌پی مرکز داده را می‌بندد. `radar_intake.py` و `radar_frames.py`
فقط محلی کار می‌کنند.

داده ماکرو از فدرال‌رزرو سنت‌لوئیس، بدون کلید.

## سبک کار

- پیش از پوش: `python -m py_compile <file>`.
- تغییر منطق امتیازدهی را با سناریوی عددی آزمون کن، نه فقط کامپایل.
  الگو: همان ورودی با داده کامل و با داده ناقص — ناقص نباید امتیاز بهتر بگیرد.
- باگ که پیدا شد، اول بپرس آیا در فایل‌های دیگر هم هست. سه بار تا حالا بوده.
- خروجی هر اسکریپت مارک‌داون است تا مستقیم در گفتگو چسبانده شود.

## کارهای باز

- `.github/workflows/radar-watch.yml` و `radar-position.yml` ساخته نشده‌اند.
- کلید رایگان CoinGecko Demo گرفته نشده.
- معامله ZEC در دفترچه ثبت نشده (ورود ۵۱۵.۷۹، ابطال ۴۸۰.۴۳).
- `radar_frames.py` هنوز در مخزن نیست.
